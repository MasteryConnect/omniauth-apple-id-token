source 'https://rubygems.org'

# Specify your gem's dependencies in omniauth-apple-id-token.gemspec
gemspec

group :test, :development do
  gem 'omniauth'
  gem 'multi_json' # For testing
  gem 'jwt' # For testing
end
